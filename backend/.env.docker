# Docker-specific defaults (override via -e if needed)

# App
APP_NAME=API Gateway
DEBUG=false
HOST=0.0.0.0
PORT=8000

# Database (Docker network service name)
DATABASE_URL=postgresql+asyncpg://gateway_user:gateway_pass_2025@postgres:5432/apigateway

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_pass_2025
REDIS_DB=0

# Security (dev only — replace in prod)
SECRET_KEY=dev-change-me
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

